# B17 Modus B — Beispiel-Umgebung. Kopieren nach .env und Passwörter ersetzen.
# Im Betrieb über das Secret-Management (ADR 0002), NICHT eingecheckt.
POSTGRES_USER=texma
POSTGRES_PASSWORD=texma
POSTGRES_DB=texma
REPLICATION_USER=replicator
REPLICATION_PASSWORD=replicator
PRIMARY_PORT=5433
STANDBY_PORT=5434
